[build-system]
requires = ["setuptools>=61", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"

[project]
name = "kagsim"
version = "0.4.0"
description = "Bit-exact C++ engine for Kaggle's Kaggriculture (1.32.7), with a batch Python API"
requires-python = ">=3.9"
license = {text = "Apache-2.0"}

[tool.setuptools]
packages = []
