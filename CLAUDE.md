# Kaggriculture — このリポジトリでの約束

2 人対戦の農業シミュレーション (Kaggle)。締切 2026-09-30、有効なのは**最新 2 提出**。

## まず読むもの

1. **`AGENTS.md`** — 方針一覧と現在地、次にやること、環境とコマンド、定型手順
2. **`.opencode/knowledge/README.md`** — ナレッジの索引
3. 触る方針の **`.opencode/knowledge/tracks/<方針>.md`** — 6 節構成。**2 節「再開の仕方」だけで動かせる**

方針は 4 つ: `chassis` (本線・提出系) / `transformer` (学習方策) / `ga` (棄却) / `planner` (棄却)。

## 毎回守ること

- **知見は `tracks/` か `refs/`、経緯は `experiments.md`。同じことを二箇所に書かない。**
  訂正したら古い記述を消す。二重に書いて訂正が片方にしか反映されない事故が実際に起きた。
- **数字を見出しにする前に SE と t を出す。** 平均値だけを見た結論は何度も覆っている。
  提出系は margin、学習方策も margin (own では順位がつかない)。val 精度は当てにならない。
- **既存系に層を足す話は、実装の前に伸びしろを測る。** シャシーを走らせながら「その層なら何をするか」を
  並べて記録するだけで上限が出る。これで丸一日の実装を節約した実績がある (`tracks/transformer.md` 6 節)。
- **A/B は調整用と判定用の seed を分ける。** 同 seed・同席のペア差で取る。
- **提出は毎回ユーザーに確認する** (1 日 5 回、最新 2 つだけ有効)。提出前に自己対戦が対称・max act < 1s・tar 展開品で動作確認。
- **公開シャシーは 1 日 1〜2 版出る。** 放置すると素の公開エージェントに負ける。手順は `refs/chassis_upgrade_playbook.md`。

## 環境

`.venv/bin/python` / `.venv/bin/kaggle`。作業データは `tmp/` (git 管理外、消えても再生成可)。
評価は `tests/kag_eval.py` (kagsim、32 戦 20 秒)。
